{
  "scripts": {
    "test": "yarn jest --coverage --bail --colors --runInBand --logHeapUsage --detectOpenHandles",
    "watch": "yarn build -w",
    "build": "./node_modules/.bin/tslint -p tsconfig.json && ./node_modules/.bin/tsc -p ."
  },
  "devDependencies": {
    "@firebase/firestore-types": "^0.7.0",
    "@types/jest": "^23.3.10",
    "@types/node": "^10.12.12",
    "firebase-tools": "^6.1.2",
    "jest": "^23.6.0",
    "ts-jest": "^23.10.5",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "typescript": "^3.2.1"
  },
  "dependencies": {
    "firebase": "^5.6.0"
  }
}
